enclave
{
    include "keyvalue.hpp"
	trusted
	{
		/*These are ECALL defines.*/
		public int ecall_start([in]KV *data, [in, out]KV *table, int *size);
			size_t message_len);
	};
	untrusted
	{
		/*These are OCALL defines.*/
		void ocall_return_stash([in]std::set<KV> *st);
	};
}
